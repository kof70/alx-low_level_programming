#include "main.h"

/**
 * a + b - function adds two integers and returns the result.
 * @a,b: the variables
 * Return: the result
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
