#include "main.h"

/**
 * add - function adds two integers and returns the result.
 * @a: the variables
 * @b: parameters
 * Return: the result
 */
int add(int a, int b)
{
	int result = a + b;

	return (result);
}
